/*
 * GuildTable Messages
 *
 * This contains all the text for the GuildTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GuildTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GuildTable component!',
  },
});
